import { test, expect } from '@playwright/test';

// Placeholder for EPAM services navigation test. Full test will be added in a follow-up commit.

test.describe('EPAM Services navigation', () => {
  test.skip('placeholder - full test to be added', async ({ page }) => {
    // placeholder
  });
});
